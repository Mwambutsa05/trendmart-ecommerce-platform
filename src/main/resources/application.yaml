spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/trendmart_ecommerce_platform
    username: postgres
    password: Ineza2005

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  flyway:
    repair-on-migrate: true

flutterwave:
  secret-key: ${FLUTTERWAVE_SECRET_KEY:default-secret-key-if-not-set}
  base-url: ${FLUTTERWAVE_BASE_URL:https://api.flutterwave.com/v3/payments}