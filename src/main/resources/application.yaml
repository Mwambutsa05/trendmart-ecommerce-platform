spring:
  profiles:
    active: prod
#    //default, prod
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/trendmart-ecommerce-platform
    password: Ineza2005
    username: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true


  flyway:
    baseline-on-migrate: true
    repair-on-migrate: true
